import 'package:flutter/material.dart';

class dev_page extends StatelessWidget {
  const dev_page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
